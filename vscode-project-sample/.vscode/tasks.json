{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "/usr/bin/crystal build --stats --debug ${relativeFile} -o bin/${fileBasenameNoExtension}",
      "problemMatcher": []
    }
  ],
}

